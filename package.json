{
  "name": "nexara",
  "lockfileVersion": 2,
  "requires": true,
  "main": "gulpfile.js",
  "scripts": {
    "archive": "cross-env git archive --format=zip --prefix=nexara/ --output=./build/nexara.zip --worktree-attributes master",
    "dev-archive": "cross-env git archive --format=zip --prefix=nexara/ --output=./build/nexara.zip --worktree-attributes dev-branch"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Syamraj24/upshift.git"
  },
  "devDependencies": {
    "cross-env": "^5.2.1",
    "gulp-sort": "^2.0.0",
    "gulp-wp-pot": "^2.5.0"
  },
  "dependencies": {
    "gulp": "^4.0.2"
  }
}
